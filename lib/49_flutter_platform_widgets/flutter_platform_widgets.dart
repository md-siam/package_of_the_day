import 'package:flutter/material.dart';

class MyFlutterPlatformWidgets extends StatelessWidget {
  const MyFlutterPlatformWidgets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Flutter Platform Widgets'),
      ),
    );
  }
}
