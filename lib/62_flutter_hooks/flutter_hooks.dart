import 'package:flutter/material.dart';

class MyFlutterHooks extends StatelessWidget {
  const MyFlutterHooks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Flutter Hooks')),
      body: Center(),
    );
  }
}
