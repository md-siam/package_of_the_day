import 'package:flutter/material.dart';

class MyPermissionHandler extends StatelessWidget {
  const MyPermissionHandler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Permission Handler')),
      body: Center(),
    );
  }
}
